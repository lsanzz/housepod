rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /stores/{storeId}/{document=**} {
      // Exemplo apenas para testes iniciais.
      // Em produção, troque por regras com autenticação.
      allow read, write: if true;
    }
  }
}
